"use strict"

const { isTrue } = require('chai').assert

////////////////////////////  using ASSERT Library  ////////////////////////////
describe("Test Suite", () => {
  it("true should be true", () => {
    // isTrue(false)
    isTrue(true)
  })
})
